<%= form_with(model: [post, post_comment] ) do |form| %>
  <% if post_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_comment.errors.count, "error") %> prohibited this post_comment from being saved:</h2>
      <ul>
      <% post_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_area :content, :placeholder => 'コメントをしてみましょう！', class:'form-control' %>
  </div>
  <div class="actions">
    <%= form.submit "コメントをする", class:"btn btn-primary" %>
  </div>
<% end %>
