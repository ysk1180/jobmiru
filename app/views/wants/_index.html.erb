
<% if user_signed_in? %>
  <% unless @post.user_id == current_user.id %>
    <% if @want.present? %>
      <%= link_to '働いてみたい！を解除する', post_want_path(post_id: @post.id, id: @want.id), method: :delete, class: 'btn btn-light btn-sm', remote: true %>
    <% else %>
      <%= link_to '働いてみたい！', post_wants_path(post_id: @post.id), method: :post, class: 'btn btn-primary btn-sm', remote: true %>
    <% end %>
  <% end %>
<% end %>
<p>働いてみたい！：<%= @wants_count %>件</p>
