<div class="container">
  <br>
  <h2>無料会員登録</h2>

  <p><i>Jobmiruの<u><a href="/terms">利用規約</a></u>および<u><a href="/privacy">プライバシーポリシー</a></u>に同意の上、会員登録お願いいたします。</i></p>
  
  <a class="btn btn-block btn-social btn-twitter" href="/users/auth/twitter">
    <span class="fa fa-twitter"></span> Twitterで新規登録
  </a>
  <small class="text-muted">Twitterに勝手に投稿されることはありません。</small>

  <br><br>

  <h6>------- or メールアドレスで登録 -------</h6>

  <br>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <!-- <%= devise_error_messages! %> -->

    <div class="form-group">
      <%= f.label :name, class: "label-inline" %><span class="badge badge-pill badge-danger">必須</span>
      <%= f.text_field :name, class:'form-control' %>
      <small class="text-muted">ニックネームをご記入下さい。</small>
    </div>

    <div class="form-group">
      <%= f.label :email, class: "label-inline" %><span class="badge badge-pill badge-danger">必須</span>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class:'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label :password, class: "label-inline" %><span class="badge badge-pill badge-danger">必須</span>
      <%= f.password_field :password, autocomplete: "off", class:'form-control' %>
      <small class="text-muted">6文字以上</small>
    </div>

    <div class="form-group">
      <%= f.label :password_confirmation, class: "label-inline" %><span class="badge badge-pill badge-danger">必須</span>
      <%= f.password_field :password_confirmation, autocomplete: "off", class:'form-control' %>
    </div>

    <div class="form-row">
      <div class="form-group col-6">
        <%= f.label :age %>
        <%= f.text_field :age, class:'form-control' %>
        <small class="text-muted">半角数字で入力してください。</small>
      </div>

      <div class="form-group col-6">
        <%= f.label :sex %>
        <%= f.select :sex,[["",""],["男性","男性"],["女性","女性"]], {}, {class: 'form-control'} %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :past_career %>
      <%= f.text_area :past_career, class:'form-control', rows:"2" %>
    </div>

    <div class="form-group">
      <%= f.label :career_plan %>
      <%= f.text_area :career_plan, class:'form-control', rows:"2" %>
    </div>

    <div class="form-group">
      <%= f.label :nickname %>
      <%= f.text_field :nickname, class:'form-control' %>
      <small class="text-muted">@に続く部分をご記入下さい。マイページにTwitterアカウントへのリンクが表示されます。</small>
    </div>

    <div class="form-group">
      <%= f.label :user_image %>
      <%= f.file_field :user_image, class:'form-control-file' %>
    </div>

    <div class="actions">
      <%= f.submit "登録", class:"btn standard-button" %>
    </div>
  <% end %>


<%= render "devise/shared/links" %>
</div>
